services:
  - type: web
    name: bancario-backend
    runtime: docker
    dockerfilePath: ./projects/bancario/backend/Dockerfile
    dockerContext: ./projects/bancario/backend
    plan: free
    healthCheckPath: /api/accounts
    envVars:
      - key: PORT
        value: 10000
